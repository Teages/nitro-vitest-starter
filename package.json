{
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite dev",
    "preview": "vite preview",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test:app": "vitest --run --coverage --project app",
    "test:server": "vitest --run --coverage --project server",
    "test": "vitest --run"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^7.7.3",
    "@vitest/coverage-v8": "^4.1.0",
    "eslint": "^10.0.3",
    "nitro": "^3.0.260311-beta",
    "vite": "^8.0.1",
    "vitest": "^4.1.0"
  }
}
